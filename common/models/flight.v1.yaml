title: Flight
type: object
properties:
  barcodeData:
    type: string
    format: binary
  checkedInStatusChanged-again:
    type: string
    enum:
      - CHECKED_IN
      - NOT_CHECKED_IN
      - BOARDED
  seatNumber-again:
    type: string
    example: F9
    pattern: '^[A-Z][1-9]$'
  seatNumber0005Changed-again:
    type: string
    example: E6
    pattern: '^[A-Z][1-9]$'
  seatNumber0007Changed-again:
    type: string
    example: T6
    pattern: '^[A-Z][1-9]$'
  seatNumber0009Changed:
    type: string
    example: T6
    pattern: '^[A-Z][1-9]$'

